import React from 'react'

const Headlines = () => {
  return (
    <section>
        {/* {articles?.map((article: string, index: number) => (
          <div key={index}>
            {article.title}
            {article.summary}
            {article.media}
          </div>
        ))} */}
    </section>
  )
}

export default Headlines